import { Route } from '@angular/router';

// Static newsletter HTML is served from /assets/newsletters/<slug>/index.html
// No client-side routes are required for now.
export const appRoutes: Route[] = [];
